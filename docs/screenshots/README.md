# Screenshots

Drop PNGs here. Current screenshots referenced by the root README:

| File | Shows |
|---|---|
| `ui.png` | WinUI 3 app — manifest review |
| `cli.png` | CLI — `msixplainer --sample` output |
| `compare.png` | WinUI 3 app — Compare page (update diff between two packages) |
| `comparecli.png` | CLI — `msixplainer diff` output |

## Capture tips

- 1600–2000 px wide, PNG, <500 KB
- Crop tight to the app window (no desktop background)
- Use the built-in sample manifest (`--sample`) for repeatable content
