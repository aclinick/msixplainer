name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    name: Build & Test (Core + CLI)
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET 10 SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'
          include-prerelease: true

      - name: Build Core
        run: dotnet build MSIXplainer.Core/MSIXplainer.Core.csproj -c Release

      - name: Build CLI
        run: dotnet build MSIXplainer.Cli/MSIXplainer.Cli.csproj -c Release

      - name: Test
        run: dotnet test MSIXplainer.Core.Tests/MSIXplainer.Core.Tests.csproj -c Release --logger "console;verbosity=normal"
